enum TaskPriority { low, medium, high, critical }

enum TaskUnit { count, minutes, hours, pages, reps }

enum HabitType { positive, negative }

enum HabitFrequency { daily, weekly, custom }

enum TransactionType { income, expense }
